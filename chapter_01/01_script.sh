#!/bin/bash
# Pound or hash sign signifies a comment (a line that is not executed)

#Command returning the current username
whoami

#Command returning the current working directory on the filesystem
pwd

# Command returning the results (file listing) of the current working
# directory
ls

# Notice the semicolon used to delimit multiple commands in the same
# execution.
echo "Echo one 1"; echo "Echo two 2"
